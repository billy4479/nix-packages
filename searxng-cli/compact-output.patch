diff --git a/src/main.rs b/src/main.rs
index 1c47f66..1d3412c 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -88,7 +88,7 @@ async fn main() -> Result<()> {
             let client = SearXNGClient::new(config);
 
             let params = SearchParams {
-                query: query.clone(),
+                query,
                 categories,
                 engines,
                 language,
@@ -98,8 +98,6 @@ async fn main() -> Result<()> {
                 safe_search,
             };
 
-            println!("🔍 Search in progress: \"{}\"...", query);
-
             match client.search(params).await {
                 Ok(response) => {
                     if compact {
@@ -178,16 +176,10 @@ async fn main() -> Result<()> {
 }
 
 fn print_compact_results(response: &searxng_client::SearXNGResponse) {
-    println!("📊 Query: {}", response.query);
-    println!("🔢 Results: {}\n", response.number_of_results);
-
     for (i, result) in response.results.iter().enumerate() {
-        println!("{}. 📄 {}", i + 1, result.title);
-        println!("   🔗 {}", result.url);
-        println!(
-            "   🏷️  Engine: {} | Category: {} | Score: {:.2}",
-            result.engine, result.category, result.score
-        );
+        println!("{}. {}", i + 1, result.title);
+        println!("   {}", result.url);
+        println!("   Score: {:.2}", result.score);
 
         if !result.content.is_empty() {
             let content = if result.content.len() > 200 {
@@ -195,16 +187,16 @@ fn print_compact_results(response: &searxng_client::SearXNGResponse) {
             } else {
                 result.content.clone()
             };
-            println!("   📝 {}", content);
+            println!("   {}", content);
         }
         println!();
     }
 
     if !response.suggestions.is_empty() {
-        println!("💡 Suggestions: {}", response.suggestions.join(", "));
+        println!("Suggestions: {}", response.suggestions.join(", "));
     }
 
     if !response.corrections.is_empty() {
-        println!("✏️  Corrections: {}", response.corrections.join(", "));
+        println!("Corrections: {}", response.corrections.join(", "));
     }
 }
